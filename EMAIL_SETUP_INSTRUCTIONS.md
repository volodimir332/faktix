# 📧 Налаштування Email для Faktix

## ⚠️ Помилка: "Invalid login: Username and Password not accepted"

Ця помилка означає що Gmail credentials не налаштовані або неправильні.

---

## ✅ Як виправити (5 хвилин)

### Крок 1: Створити Gmail App Password

1. **Перейдіть на:** https://myaccount.google.com/apppasswords
2. **Увійдіть** в Google аккаунт: `xperementus@gmail.com`
3. **Важливо:** Для використання App Passwords потрібна **2-Step Verification**
   - Якщо не увімкнена, спочатку увімкніть тут: https://myaccount.google.com/signinoptions/two-step-verification
4. **Створіть новий App Password:**
   - App name: `Faktix Email`
   - Device: `Other (Custom name)`
   - Натисніть **Generate**
5. **Скопіюйте** згенерований пароль (формат: `xxxx xxxx xxxx xxxx`)

---

### Крок 2: Додати credentials в .env.local

**Відкрийте термінал:**

```bash
cd /Users/volodymyrkrutskyi/Desktop/fakrury/faktury
nano .env.local
```

**Додайте ці рядки в КІНЕЦЬ файлу:**

```env
# Email Configuration (Nodemailer)
EMAIL_USER=xperementus@gmail.com
EMAIL_PASS=your-app-password-here
```

**Замініть** `your-app-password-here` на пароль з Кроку 1

**Збережіть:**
- Натисніть `Ctrl+O` (Write Out)
- Натисніть `Enter`
- Натисніть `Ctrl+X` (Exit)

---

### Крок 3: Перезапустити сервер

**Термінал:**

```bash
# Зупинити поточний сервер (Ctrl+C в терміналі де запущено npm run dev)
# Або:
cd /Users/volodymyrkrutskyi/Desktop/fakrury/faktury
lsof -ti:3000 | xargs kill -9 2>/dev/null || true
npm run dev
```

---

## 🧪 Тестування

1. Оновіть браузер (Cmd+Shift+R)
2. Створіть калькуляцію
3. Натисніть "Send" (відправити)
4. Введіть email
5. Натисніть "Odeslat rozpočet"

**Очікуваний результат:**
```
✅ Email має відправитись
✅ Побачите: "✅ Kalkulace byla úspěšně odeslána..."
✅ Email прийде на вказану адресу з PDF вкладенням
```

---

## 📋 Альтернативний спосіб (якщо nano не зручний)

**Використайте будь-який текстовий редактор:**

```bash
open -a TextEdit /Users/volodymyrkrutskyi/Desktop/fakrury/faktury/.env.local
```

Додайте в кінець файлу:
```
EMAIL_USER=xperementus@gmail.com
EMAIL_PASS=your-app-password-here
```

Збережіть та закрийте.

---

## 🔍 Як перевірити що працює?

**Термінал (консоль сервера):**

При відправці email має з'явитись:
```
📧 Sending email to: client@example.com
✅ SMTP connection verified
✅ Email sent successfully: <message-id>
```

**Якщо побачите:**
```
❌ Invalid login: 535-5.7.8 Username and Password not accepted
```

Означає що:
- App Password неправильний
- Не скопійовано повністю (має бути 16 символів)
- 2-Step Verification не увімкнена

---

## 🔐 Безпека

⚠️ **НІКОЛИ НЕ КОМІТЬТЕ .env.local в Git!**

Файл вже додано в `.gitignore`:
```
.env*.local
.env.local
```

---

## 📚 Додаткова інформація

### Gmail App Password формат:
```
Правильно:  abcd efgh ijkl mnop  (16 символів, пробіли можна прибрати)
Правильно:  abcdefghijklmnop     (без пробілів теж працює)
Неправильно: abcd****ijkl****    (не весь пароль)
```

### Gmail ліміти:
- 500 emails/день для безкоштовного аккаунта
- 100 recipients на email
- Розмір вкладення: до 25 MB

### Альтернативи:
Якщо Gmail не працює, можна використати:
- **SendGrid** (безкоштовно 100 emails/день)
- **Mailgun** (безкоштовно 100 emails/день)
- **Resend** (безкоштовно 100 emails/день)

---

## ❓ FAQ

**Q: Чому не працює звичайний пароль Gmail?**  
A: Google вимагає App Password для безпеки. Звичайний пароль не працює в SMTP.

**Q: Де знайти App Password якщо я його забув?**  
A: Не можна подивитись старі. Треба видалити старий та створити новий.

**Q: Чи потрібно налаштовувати щось в Gmail Settings?**  
A: Ні, тільки увімкнути 2-Step Verification та створити App Password.

**Q: Чи працює з іншими email провайдерами?**  
A: Так, але потрібно змінити SMTP settings в `route.ts`:
- Outlook: `smtp.office365.com:587`
- Yahoo: `smtp.mail.yahoo.com:465`
- Custom: використовуйте свої SMTP дані

---

**Створено:** 11 листопада 2025  
**Статус:** Інструкції готові  
**Час налаштування:** ~5 хвилин


