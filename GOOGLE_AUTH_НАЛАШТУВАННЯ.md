# 🔐 Налаштування Google Автентифікації для faktix.cz

## ✅ ЩО ВЖЕ ЗРОБЛЕНО

Google автентифікація **вже реалізована в коді**:
- ✅ Компонент `GoogleSignInButton` створено
- ✅ Функція `signInWithGoogle()` реалізована
- ✅ Кнопка додана на сторінки `/prihlaseni` та `/registrace`
- ✅ Firebase Authentication налаштовано

## 🚨 ЧОМУ НЕ ВІДОБРАЖАЄТЬСЯ НА ДОМЕНІ?

Є **3 основні причини**:

### 1️⃣ Код не задеплоєний на продакшн
### 2️⃣ Firebase Console не налаштований для Google провайдера
### 3️⃣ Домен faktix.cz не додано в авторизовані домени Firebase

---

## 📝 ПОКРОКОВА ІНСТРУКЦІЯ

### КРОК 1: Налаштування Firebase Console (10 хвилин)

#### 1.1 Відкрийте Firebase Console
1. Перейдіть: https://console.firebase.google.com/
2. Оберіть ваш проект: **faktix-8d2cc**

#### 1.2 Увімкніть Google як провайдера
1. В лівому меню виберіть **"Authentication"** (🔐 Автентифікація)
2. Перейдіть на вкладку **"Sign-in method"** (Методи входу)
3. Знайдіть **"Google"** в списку провайдерів
4. Натисніть на **"Google"**
5. Увімкніть перемикач **"Enable"** (Увімкнути)
6. В полі **"Project support email"** оберіть вашу email адресу
7. Натисніть **"Save"** (Зберегти)

✅ **Результат**: Google з'явиться в списку з зеленою галочкою

#### 1.3 Додайте авторизовані домени
1. На тій же сторінці **"Sign-in method"**
2. Прокрутіть вниз до розділу **"Authorized domains"** (Авторизовані домени)
3. Натисніть **"Add domain"** (Додати домен)
4. Введіть: `faktix.cz`
5. Натисніть **"Add"** (Додати)
6. Повторіть для: `www.faktix.cz` (якщо використовуєте)

✅ **Результат**: В списку мають бути:
- localhost (для розробки)
- faktix-8d2cc.firebaseapp.com (автоматично)
- faktix.cz (ваш домен)
- www.faktix.cz (якщо потрібно)

#### 1.4 Перевірте налаштування
1. На сторінці **"Sign-in method"**
2. Переконайтеся, що:
   - ✅ Email/Password - увімкнено
   - ✅ Google - увімкнено
   - ✅ faktix.cz в авторизованих доменах

---

### КРОК 2: Деплой коду на продакшн (5 хвилин)

Ви маєте **незакомічені зміни** в Git. Потрібно їх задеплоїти:

#### 2.1 Закомітьте зміни
```bash
cd /Users/volodymyrkrutskyi/Desktop/fakrury/faktury

# Додайте всі зміни
git add .

# Закомітьте з описом
git commit -m "feat: додано Google Authentication"

# Відправте на сервер
git push origin main
```

#### 2.2 Перевірте деплой (якщо у вас Vercel/Netlify)
1. Відкрийте ваш hosting dashboard (Vercel, Netlify, тощо)
2. Перевірте, що деплой успішний
3. Зачекайте 2-3 хвилини на завершення

✅ **Результат**: Новий код буде на домені faktix.cz

---

### КРОК 3: Перевірка роботи (2 хвилини)

#### 3.1 Відкрийте сторінку входу
1. Перейдіть: https://faktix.cz/prihlaseni
2. Прокрутіть вниз

#### 3.2 Має бути видно:
```
┌─────────────────────────────────────────┐
│                                         │
│  Або се přihlaste pomocí                │
│                                         │
│  ┌───────────────────────────────────┐ │
│  │  [G] Prodovжити з Google         │ │
│  └───────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

#### 3.3 Натисніть на кнопку Google
Має відкритися:
1. Popup вікно Google
2. Вибір акаунту Google
3. Підтвердження доступу
4. Автоматичний вхід в систему

✅ **Результат**: Ви увійшли через Google!

---

## 🔍 ДІАГНОСТИКА ПРОБЛЕМ

### Проблема 1: "Не бачу кнопку Google"

**Перевірте:**
1. Чи задеплоєний код? (git push origin main)
2. Чи завершився деплой на хостингу?
3. Відкрийте консоль браузера (F12)
4. Перевірте, чи є помилки

**Рішення:**
```bash
# Перевірте, чи є компонент в коді
grep -r "GoogleSignInButton" src/app/prihlaseni/

# Має показати:
# src/app/prihlaseni/page.tsx:import { GoogleSignInButton } from "@/components/GoogleSignInButton";
# src/app/prihlaseni/page.tsx:                <GoogleSignInButton />
```

### Проблема 2: "Кнопка є, але не працює"

**Перевірте Firebase Console:**
1. Authentication → Sign-in method
2. Google має бути **увімкнений** (зелена галочка)
3. faktix.cz має бути в **Authorized domains**

**Помилка в консолі:**
```
auth/unauthorized-domain
```

**Рішення:** Додайте faktix.cz в Authorized domains (див. КРОК 1.3)

### Проблема 3: "Popup вікно закривається відразу"

**Причина:** Popup блокується браузером

**Рішення:**
1. Дозвольте popup для faktix.cz
2. Спробуйте в іншому браузері
3. Перевірте Safari налаштування (якщо Safari)

---

## 📱 ЯК ВИГЛЯДАЄ В КОДІ

### Сторінка входу (`src/app/prihlaseni/page.tsx`):
```tsx
// Рядок 10: Імпорт компонента
import { GoogleSignInButton } from "@/components/GoogleSignInButton";

// Рядки 223-237: Розділювач
<div className="mt-6">
  <div className="relative">
    <div className="absolute inset-0 flex items-center">
      <div className="w-full border-t border-gray-600" />
    </div>
    <div className="relative flex justify-center text-sm">
      <span className="px-2 bg-black text-gray-400">Nebo se přihlaste pomocí</span>
    </div>
  </div>

  {/* Рядок 234-236: Кнопка Google */}
  <div className="mt-6">
    <GoogleSignInButton />
  </div>
</div>
```

### Компонент (`src/components/GoogleSignInButton.tsx`):
```tsx
export function GoogleSignInButton() {
  const { signInWithGoogle } = useAuth();
  const router = useRouter();
  
  const handleGoogleSignIn = async () => {
    const result = await signInWithGoogle();
    if (result.success) {
      router.push('/dashboard');
    }
  };

  return (
    <button onClick={handleGoogleSignIn}>
      <svg>{/* Google logo */}</svg>
      <span>Prodovжити з Google</span>
    </button>
  );
}
```

### Firebase функція (`src/lib/firebase-auth.ts`):
```tsx
export const signInWithGoogle = async () => {
  const provider = new GoogleAuthProvider();
  const userCredential = await signInWithPopup(auth, provider);
  return { success: true, user: userCredential.user };
};
```

---

## ✅ ЧЕК-ЛИСТ ДЛЯ ПЕРЕВІРКИ

Відмічайте кожен пункт:

- [ ] Відкрив Firebase Console
- [ ] Знайшов проект faktix-8d2cc
- [ ] Перейшов в Authentication → Sign-in method
- [ ] Увімкнув Google провайдера
- [ ] Вказав Project support email
- [ ] Додав faktix.cz в Authorized domains
- [ ] Додав www.faktix.cz в Authorized domains (якщо потрібно)
- [ ] Закомітив зміни в Git (git add . && git commit)
- [ ] Відправив на сервер (git push origin main)
- [ ] Перевірив деплой на хостингу
- [ ] Відкрив https://faktix.cz/prihlaseni
- [ ] Побачив кнопку Google
- [ ] Натиснув на кнопку Google
- [ ] Popup відкрився
- [ ] Вибрав акаунт Google
- [ ] Увійшов успішно

**Після виконання всіх пунктів - Google Authentication працює! 🎉**

---

## 🚀 ДОДАТКОВІ РЕКОМЕНДАЦІЇ

### Для покращення UX:

1. **Додайте Facebook/Apple Sign In** (опціонально)
2. **Додайте аватар користувача** з Google акаунту
3. **Збережіть email** з Google в профілі
4. **Автоматично заповнюйте ім'я** з Google акаунту

### Для безпеки:

1. **Перевірте email verification** після входу через Google
2. **Додайте двофакторну автентифікацію** (2FA)
3. **Логуйте всі входи** для аналітики

---

## 📞 ЯКЩО ЩОСЬ НЕ ВИХОДИТЬ

1. **Перевірте консоль браузера** (F12 → Console)
2. **Перевірте Firebase Console** (Authentication налаштування)
3. **Перевірте, чи код задеплоєний** (git log --oneline -5)
4. **Спробуйте інший браузер** (Chrome, Firefox, Safari)
5. **Почистіть кеш браузера** (Cmd+Shift+R на Mac)

---

**Після виконання цих кроків Google Authentication буде працювати на faktix.cz! 🎯**

Якщо у вас залишаються питання - напишіть мені, і я допоможу!

