# AI-Бухгалтер - Knowledge Base

## 🎯 Огляд

AI-Бухгалтер для чеської податкової системи з повною базою знань з офіційних джерел:
- ✅ Finanční správa ČR (податки, e-Tax)
- ✅ ČSSZ (соціальне страхування)
- ✅ MFČR (Міністерство фінансів)
- ✅ e-Sbírka (закони ЧР)

## 📚 Архітектура

```
src/lib/kb/
├── types.ts          # TypeScript інтерфейси
├── constants.ts      # Конфігурація джерел та промпти
├── embeddings.ts     # OpenAI embeddings
├── chunker.ts        # Розбиття документів на chunks
├── firestore.ts      # Firebase/Firestore інтеграція
├── rag.ts            # RAG система (Retrieval Augmented Generation)
└── scraper.ts        # Web scraper для офіційних джерел
```

## 🚀 Швидкий старт

### 1. Налаштування змінних середовища

Скопіюйте `.env.example` в `.env.local`:

```bash
cp .env.example .env.local
```

Заповніть змінні:
```env
# DeepSeek API (для AI відповідей)
DEEPSEEK_API_KEY=sk-xxxxx

# OpenAI API (для embeddings)
OPENAI_API_KEY=sk-xxxxx
```

### 2. Перший запуск - завантаження базової KB

```bash
# Запустити dev server
npm run dev

# В іншому терміналі - завантажити базову KB
curl -X POST http://localhost:3000/api/kb/scrape \
  -H "Content-Type: application/json" \
  -d '{"action": "scrape_single", "source": "cssz"}'
```

### 3. Використання AI-чату

1. Відкрийте розділ **Účetnictví** (`/ucetnictvi`)
2. Натисніть кнопку **"AI Бухгалтер"** у правому верхньому куті
3. Задайте питання або оберіть готове з підказок

## 📖 API Endpoints

### `/api/ai-accountant` (POST)
Запит до AI-бухгалтера

**Request:**
```json
{
  "question": "Як порахувати соціальне страхування для OSVČ?",
  "language": "uk",
  "userContext": {
    "businessType": "osvc",
    "annualIncome": 500000,
    "isPausalni": false
  }
}
```

**Response:**
```json
{
  "answer": "Детальна відповідь...",
  "sources": [
    {
      "title": "ČSSZ - Sociální pojištění OSVČ",
      "url": "https://www.cssz.cz/...",
      "relevantText": "..."
    }
  ],
  "confidence": 0.92,
  "chunks": [...]
}
```

### `/api/kb/scrape` (POST)
Завантажити дані з офіційних джерел

**Scrape all sources:**
```bash
curl -X POST http://localhost:3000/api/kb/scrape \
  -H "Content-Type: application/json" \
  -d '{"action": "scrape_all"}'
```

**Scrape single source:**
```bash
curl -X POST http://localhost:3000/api/kb/scrape \
  -H "Content-Type: application/json" \
  -d '{"action": "scrape_single", "source": "financni_sprava"}'
```

### `/api/kb/stats` (GET)
Статистика бази знань

```bash
curl http://localhost:3000/api/kb/stats
```

## 🔄 Оновлення Knowledge Base

### Автоматичне оновлення (рекомендовано)

Налаштуйте cron job для щоденного оновлення:

```bash
# Додати в crontab (запускати щодня о 3:00)
0 3 * * * curl -X POST http://your-domain.com/api/kb/scrape \
  -H "Content-Type: application/json" \
  -d '{"action": "scrape_all"}'
```

### Ручне оновлення

1. Відкрийте термінал
2. Виконайте команду:
```bash
npm run kb:update
```

## 📊 Джерела даних

### 1. Finanční správa (`financni_sprava`)
- **URL**: https://financnisprava.gov.cz
- **Категорії**: Daně z příjmů, DPH, e-Tax, Paušální daň
- **Оновлення**: Щоденно

### 2. ČSSZ (`cssz`)
- **URL**: https://www.cssz.cz
- **Категорії**: Sociální pojištění, minimální zálohy, termíny
- **Оновлення**: Щоденно

### 3. MFČR (`mfcr`)
- **URL**: https://www.mfcr.cz
- **Категорії**: Daňové pokyny, účetnictví
- **Оновлення**: Щотижня

### 4. e-Sbírka (`e_sbirka`)
- **URL**: https://www.e-sbirka.cz
- **Категорії**: Zákony (586/1992, 235/2004, 589/1991, 48/1997)
- **Оновлення**: При зміні законів

## 🧪 Тестові запитання

### Українською:
1. "Як порахувати авансові платежі на соціальне страхування для OSVČ?"
2. "Які терміни подання податкової декларації 2025?"
3. "Коли треба реєструватися на ПДВ?"
4. "Яка різниця між паушальним податком та звичайним режимом?"
5. "Які мінімальні авансові платежі на страхування?"

### Чеською:
1. "Jak vypočítat zálohy na sociální pojištění pro OSVČ?"
2. "Jaké jsou termíny podání daňového přiznání 2025?"
3. "Kdy se musím registrovat k DPH?"
4. "Jaký je rozdíl mezi paušální daní a běžným režimem?"
5. "Jak vysoké jsou minimální zálohy na pojistné?"

## 🛠️ Налаштування

### Chunking Configuration

У `src/lib/kb/constants.ts`:

```typescript
export const CHUNKING_CONFIG = {
  maxTokens: 800,      // Розмір chunk
  overlap: 100,        // Перекриття між chunks
  strategy: 'semantic' // sentence | paragraph | semantic
};
```

### RAG Configuration

```typescript
export const RAG_CONFIG = {
  maxRetrievedChunks: 5,      // Скільки chunks витягувати
  minSimilarityScore: 0.7,    // Мінімальна схожість
  maxContextTokens: 3000,     // Максимум токенів контексту
  temperature: 0.3,           // Температура AI (точність)
};
```

### Embeddings Configuration

```typescript
export const EMBEDDING_CONFIG = {
  model: 'text-embedding-3-small',  // OpenAI model
  dimensions: 1536,                 // Розмірність векторів
  batchSize: 100,                   // Batch розмір
};
```

## 🔍 Моніторинг якості

### Перевірка відповідей

1. Відкрийте `/api/kb/stats` для перегляду статистики
2. Протестуйте на тестових запитаннях
3. Перевірте `confidence` score (має бути > 0.7)
4. Переконайтеся, що є посилання на джерела

### Логи

Всі операції логуються в консоль:
- ✅ Успішні операції (зелений)
- ⚠️ Попередження (жовтий)
- ❌ Помилки (червоний)

## 🚧 Відомі обмеження

1. **Web scraping**: Простий HTML parser - може не працювати з динамічними сторінками
   - **Рішення**: Використати Puppeteer або Playwright для складних сайтів

2. **Vector search**: Firestore не має нативної підтримки vector search
   - **Рішення**: Для production використати Pinecone або Qdrant

3. **Мовна модель**: DeepSeek може не розуміти всі чеські терміни
   - **Рішення**: Fallback на OpenAI GPT-4

## 📈 Наступні кроки (Phase 2)

- [ ] **Puppeteer integration** для динамічних сайтів
- [ ] **PDF parsing** для законів та форм
- [ ] **Pinecone/Qdrant** для швидшого vector search
- [ ] **Admin panel** для керування KB
- [ ] **A/B testing** якості відповідей
- [ ] **Multi-user chat history** збереження історії

## 🤝 Підтримка

Питання? Напишіть у Telegram або email.

## 📝 Ліцензія

Proprietary - Faktix Platform © 2025

