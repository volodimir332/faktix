# 🚨 Швидке вирішення помилки Firebase auth/configuration-not-found

## ❌ Проблема
При реєстрації з'являється помилка: `Firebase: Error (auth/configuration-not-found)`

## 🔧 Швидке вирішення (5 хвилин)

### Крок 1: Перевірте Firebase Console
1. Відкрийте: https://console.firebase.google.com/
2. Увійдіть в свій Google акаунт
3. Виберіть проект `faktix-8d2cc`

### Крок 2: Увімкніть Authentication
1. В лівому меню натисніть **Authentication**
2. Натисніть **Get started**
3. Перейдіть на вкладку **Sign-in method**
4. Знайдіть **Email/Password** і натисніть **Enable**
5. Натисніть **Save**

### Крок 3: Створіть Firestore Database
1. В лівому меню натисніть **Firestore Database**
2. Натисніть **Create database**
3. Виберіть **Start in test mode**
4. Виберіть регіон **europe-west3 (Prague)**
5. Натисніть **Done**

### Крок 4: Перевірте Web App
1. Натисніть **Project Settings** (шестерня)
2. В розділі **Your apps** перевірте, чи є web app
3. Якщо немає, натисніть **Add app** → **Web**
4. Введіть **App nickname**: `faktix-web`
5. Натисніть **Register app**

### Крок 5: Тестування
1. Відкрийте: http://localhost:3000/test-firebase-config
2. Натисніть **Run Firebase Test**
3. Перевірте результати

## 🎯 Альтернативне рішення - Новий проект

### Якщо вище не допомогло:

#### Крок 1: Створіть новий проект
1. В Firebase Console натисніть **Create a project**
2. **Project name**: `faktix-new`
3. **Enable Google Analytics**: No
4. Натисніть **Create project**

#### Крок 2: Налаштуйте новий проект
1. **Authentication** → **Get started** → **Email/Password** → **Enable**
2. **Firestore Database** → **Create database** → **Test mode** → **europe-west3**
3. **Project Settings** → **Add app** → **Web** → **faktix-web**

#### Крок 3: Оновіть конфігурацію
1. Скопіюйте нову конфігурацію з Firebase Console
2. Оновіть файл `src/lib/firebase.ts`
3. Перезапустіть сервер: `npm run dev`

## 🔍 Діагностика

### Перевірте консоль браузера:
1. Натисніть F12
2. Перейдіть на вкладку Console
3. Знайдіть повідомлення з 🔧 та ✅

### Очікувані повідомлення:
```
🔧 Firebase config check: { hasApiKey: true, ... }
✅ Firebase app initialized successfully
✅ Firebase Auth initialized
✅ Firestore initialized
```

### Помилки для вирішення:
- `auth/configuration-not-found` → Перевірте Firebase Console
- `auth/operation-not-allowed` → Увімкніть Email/Password
- `auth/invalid-api-key` → Перевірте API ключ

## 📞 Якщо нічого не допомогло

1. **Створіть новий проект Firebase**
2. **Використовуйте Firebase Emulator** для розробки
3. **Перевірте мережу** та брандмауер
4. **Спробуйте інший браузер**

## ✅ Результат

Після виконання всіх кроків:
- ✅ Реєстрація працює без помилок
- ✅ Дані зберігаються в Firestore
- ✅ Авторизація працює правильно
- ✅ Консоль показує ✅ повідомлення

---

**Час виконання: 5-10 хвилин**
**Складність: Легко**
**Статус: Критично важливо**
