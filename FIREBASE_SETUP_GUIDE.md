# 🔥 Firebase Setup Guide - Виправлення помилки auth/configuration-not-found

## 🚨 Проблема
Ви отримуєте помилку: `Firebase: Error (auth/configuration-not-found)`

Це означає, що Firebase проект не налаштований правильно або не існує.

## ✅ Рішення - Покрокові інструкції

### Крок 1: Створення Firebase проекту

1. **Відкрийте Firebase Console:**
   - Перейдіть на https://console.firebase.google.com/
   - Увійдіть з вашим Google акаунтом

2. **Створіть новий проект:**
   - Натисніть "Create a project" або "Add project"
   - Введіть назву: `faktix-8d2cc` (або іншу)
   - Можете вимкнути Google Analytics
   - Натисніть "Create project"

### Крок 2: Налаштування Authentication

1. **Увімкніть Email/Password аутентифікацію:**
   - У Firebase Console перейдіть до "Authentication"
   - Натисніть "Get started"
   - Перейдіть на вкладку "Sign-in method"
   - Натисніть на "Email/Password"
   - Увімкніть "Email/Password"
   - Натисніть "Save"

### Крок 3: Створення Web додатку

1. **Додайте Web додаток:**
   - У Firebase Console натисніть на іконку веб-сайту (</>)
   - Введіть назву додатку: `Faktix Web App`
   - Можете вимкнути Firebase Hosting
   - Натисніть "Register app"

2. **Скопіюйте конфігурацію:**
   - Firebase покаже вам конфігурацію
   - Скопіюйте її та замініть в файлі `src/lib/firebase.ts`

### Крок 4: Оновлення конфігурації

Замініть конфігурацію в файлі `src/lib/firebase.ts`:

```typescript
const firebaseConfig = {
  apiKey: "ВАШ_API_KEY",
  authDomain: "ВАШ_PROJECT_ID.firebaseapp.com",
  projectId: "ВАШ_PROJECT_ID",
  storageBucket: "ВАШ_PROJECT_ID.appspot.com",
  messagingSenderId: "ВАШ_SENDER_ID",
  appId: "ВАШ_APP_ID"
};
```

### Крок 5: Налаштування Firestore Database

1. **Створіть базу даних:**
   - У Firebase Console перейдіть до "Firestore Database"
   - Натисніть "Create database"
   - Виберіть "Start in test mode"
   - Виберіть регіон (наприклад, europe-west3)
   - Натисніть "Done"

### Крок 6: Перевірка налаштувань

1. **Перезапустіть сервер:**
   ```bash
   npm run dev
   ```

2. **Відкрийте консоль браузера:**
   - Натисніть F12
   - Перейдіть на вкладку Console
   - Перевірте, чи є повідомлення про Firebase

3. **Спробуйте реєстрацію:**
   - Перейдіть на http://localhost:3000/registrace
   - Спробуйте зареєструватися

## 🔧 Альтернативне рішення - Використання існуючого проекту

Якщо у вас вже є Firebase проект, але він налаштований неправильно:

### Перевірте налаштування проекту:

1. **Перейдіть до налаштувань проекту:**
   - У Firebase Console натисніть на шестерню ⚙️
   - Виберіть "Project settings"

2. **Перевірте Web додатки:**
   - Перейдіть на вкладку "General"
   - Прокрутіть вниз до "Your apps"
   - Якщо немає Web додатку, створіть новий

3. **Перевірте Authentication:**
   - Перейдіть до "Authentication" → "Sign-in method"
   - Переконайтеся, що Email/Password увімкнено

## 🚀 Швидке тестування

Після налаштування:

1. **Відкрийте тестову сторінку:**
   - http://localhost:3000/test-firebase

2. **Перевірте консоль браузера:**
   - Повинні бути повідомлення:
     - ✅ Firebase app initialized successfully
     - ✅ Firebase Auth initialized
     - ✅ Firestore initialized

3. **Спробуйте реєстрацію:**
   - Введіть тестові дані
   - Перевірте, чи немає помилок

## 📞 Якщо проблема залишається

1. **Перевірте консоль браузера** - там будуть детальні повідомлення про помилки
2. **Перевірте Firebase Console** - чи правильно налаштований проект
3. **Перевірте конфігурацію** - чи всі ключі правильні
4. **Перезапустіть сервер** після зміни конфігурації

---

**Після виконання цих кроків помилка `auth/configuration-not-found` повинна зникнути!** 🎉
